import React, { Component } from 'react';

export class Main extends Component {
  static displayName = Main.name;

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        메인
      </div>
    );
  }
}